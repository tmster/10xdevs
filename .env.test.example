# Test Environment
BASE_URL=http://localhost:3001

# Supabase Configuration
SUPABASE_URL=your_supabase_url
SUPABASE_PUBLIC_KEY=your_supabase_anon_key

# Test User Credentials for authentication
# These credentials should belong to a test user in your Supabase instance
E2E_USERNAME=test@example.com
E2E_PASSWORD=test_password
E2E_USER_ID=user_id_of_test_account

# Do NOT use service role keys in tests - use proper authentication instead